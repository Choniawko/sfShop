{% extends "::base.html.twig" %}

{% block title %}Opis produktu{% endblock %}

{% block body %}

<div class="col-md-3 col-lg-3">
<a href="#" class="thumbnail">
      <img src="http://placehold.it/240x330" alt="" class="img-thumbnail">
    </a>
</div>

<div class="col-md-6 col-lg-6">

    <h1>{{ product.name }}</h1>
    {{ product.description }}
    <div class="thumbnail">
   
        <h2>{{ product.price }} zł<span style="padding-left:150px"><a href="{{ path('basket_add', {'id': product.id}) }}" class="btn btn-warning" role="button">Dodaj do koszyka</a></span></h2>
                   
    </div>
    <a href="{{ path('products_list') }}" class="btn btn-primary">Kupuj dalej</a>
</div>
<div class="col-md-12"></div> 
{% endblock %}